import 'package:flutter/material.dart';


void main() => runApp(twoFAPage());

class twoFAPage extends StatelessWidget {
  const twoFAPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
